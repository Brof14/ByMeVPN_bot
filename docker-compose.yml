version: '3.8'

services:
  vpnbot:
    build: .
    container_name: vpnbot
    restart: unless-stopped
    env_file:
      - .env
    network_mode: host
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
